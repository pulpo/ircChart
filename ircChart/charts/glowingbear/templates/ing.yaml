{{- $values := .Values.ingress -}}
apiVersion: networking.k8s.io/v1 
kind: Ingress
metadata:
  name: {{ template "gb.fullname" . }}
  annotations:
  {{- if $values.annotations }}
  annotations:
    {{- with $values.annotations }}
    {{- toYaml . | nindent 4 }}
    {{- end }}
  {{- end }}
spec:
  tls:
  - hosts:
    - {{ .Values.hostUrl }}
    secretName: "{{ template "gb.fullname" . }}-cert"
  rules:
  - host: {{ .Values.hostUrl }}
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: {{ template "gb.fullname" . }}
            port: 
              number: 80
